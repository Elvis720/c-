#include <iostream>
#include <string>

using namespace std;

int main(){
    int n;
    string str;
    cin >> n;
    str = to_string(n);
    int len = str.size();
    for(int i = len -1;i >-1;i--){
        for()
    }
    return 0;
}
